# Stonefellow uploads

Admin Media Upload + Storage v1 stores uploaded files under this folder by year/month.

Keep this folder writable by the web server in local/staging installs. For production video at scale, use the media asset registry with CDN/streaming URLs instead of storing large video files on the app server.
