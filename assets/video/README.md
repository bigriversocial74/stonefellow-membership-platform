# Stonefellow video assets

Place production video files here after encoding/upload workflow is finalized.

Expected starter paths used by the demo pages and SQL seed:

- `assets/video/episodes/first-to-fall.mp4`
- `assets/video/previews/first-to-fall-preview.mp4`
- `assets/video/trailers/first-to-fall-trailer.mp4`

The current build is wired for access checks, watch pages, and tracking APIs even before real MP4 files are added.
