# Runtime Config

The web installer writes `config/local.php` here with database and site settings.

`config/local.php` is ignored by Git because it contains database credentials.

Keep this directory writable during installation. After install, permissions can be tightened.
