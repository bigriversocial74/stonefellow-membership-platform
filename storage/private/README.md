# Private runtime storage

Stonefellow writes the local product activation receipt to this directory during setup.

Do not commit or distribute generated files from this directory. The release package should include only this README so the directory can be created with writable permissions.
